name: Release Please & GoReleaser

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  release:
    uses: divmora/.github/.github/workflows/go-release.yml@main
    secrets: inherit
